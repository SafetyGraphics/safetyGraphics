context("Tests for the validateSetting() function")
library(ReDish)

# a boolean variable called `status` is returned

# a list `variableStatus` containing a status report for each data mapping setting is returned

# numeric variables called `settingVariablesFound` and `settingVariablesMissing` are returned

# a character variable called `validationSummary` is returned

# data frame where all columns and fields are found is marked valid

# data frame with a missing column is marked invalid

# data frame with a missing field is marked invalid

# passing an invalid data object throws an error

